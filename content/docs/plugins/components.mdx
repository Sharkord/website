---
title: Components
description: Build custom UI components for Sharkord.
icon: Puzzle
---

Plugins can create custom UI components that integrate directly into the Sharkord interface. This allows you to build rich, interactive features that feel native to the app.

### Slots

Sharkord provides several "slots" where plugins can inject their components. These include:

- `CONNECT_SCREEN`: A slot below the logo and server name on the connect screen.
- `HOME_SCREEN`: A slot on the home screen, which is the screen the user sees when he connects to the server.
- `CHAT_ACTIONS`: A slot in the chat input area, where plugins can add buttons or other interactive elements.
- `TOPBAR_RIGHT`: A slot on the top bar, on the right side of the screen, next to the existing buttons.
- `FULL_SCREEN`: A slot that takes over the entire screen and adds a button to the left sidebar to open it. This is useful for plugins that need a lot of space or want to create a custom interface.

Each plugin can render multiple components in different slots.

### Building Components

Build components with the Sharkord UI library and export your slot map from the client entrypoint.

For example, to create a simple button component that can be rendered in the `CHAT_ACTIONS` slot:

```tsx
// src/client/index.ts

import {
  PluginSlot,
  type TPluginComponentsMapBySlotId,
} from "@sharkord/plugin-sdk";
import { HomeTest } from "./components/test";

const components: TPluginComponentsMapBySlotId = {
  [PluginSlot.HOME_SCREEN]: [HomeTest],
};

export { components };
```

In `src/server/index.ts` you also need to enable the components feature:

```tsx
// src/server/index.ts

import { type PluginContext, type UnloadPluginContext } from "@sharkord/plugin-sdk";

const onLoad = async (ctx: PluginContext) => {
  ctx.ui.enable();
};

export { onLoad };
```

Call `ctx.ui.enable()` when your plugin ships components.

### Styling

Keep styling simple. Prefer Sharkord UI primitives first, and do not assume every Tailwind utility will be available in the host app.

If a component needs privileged behavior, call a server [action](./actions).

### Example

In this example, we created a simple component that shows in the home screen.

![Example of a plugin rendering a button in the chat actions slot](/Screenshot_20260218_214904.png)
